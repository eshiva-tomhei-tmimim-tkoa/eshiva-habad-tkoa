auto-install-peers=true
strict-peer-dependencies=false
# Устойчивость к транзиентным сетевым сбоям (особенно в Docker-сборке)
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
fetch-timeout=300000
